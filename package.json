{
  "name": "react-app-with-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "parcel ./*.html",
    "predeploy": "rm -rf dist && rm -rf .parcel-cache/ &&  parcel build ./*.html --public-url ./",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "bootstrap-icons": "^1.11.3",
    "dompurify": "^3.1.4",
    "highlight.js": "^11.9.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "process": "^0.11.10"
  }
}
